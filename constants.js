const MNEMONIC = "your mnemonic here"; 
const NUM_MINTS = 1; // Number of mints to perform
const CONTRACT_ADRESS = "your contract address here"; // Contract address
const MINT_PRICE = 200; // Mint price in stars
const START_HOUR = 12; // Start hour in a 24h format
const START_MINUTE = 0; // Start minute

module.exports = { MNEMONIC, NUM_MINTS, CONTRACT_ADRESS, MINT_PRICE, START_HOUR, START_MINUTE};